version: 1
batch_limit: 10
simulated_retention_days: 1
cache_ttl_seconds: 3600
rerun_policy: resume_confirmed
authorization_model: single_human_authorizer
incompatible_roles:
  - [package_preparer, final_authorizer]
probable_resolution_policy: human_required
required_authorizer_count: 1
