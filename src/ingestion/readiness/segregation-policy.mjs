import { PromotionBlockedError } from '../promotion/proposal-contract.mjs';
export function checkSegregation(assignments, policy) { const byId=new Map(); for(const assignment of assignments){const roles=byId.get(assignment.identity)??new Set();roles.add(assignment.role);byId.set(assignment.identity,roles);} for(const roles of byId.values())for(const [a,b] of policy.incompatible_roles)if(roles.has(a)&&roles.has(b))throw new PromotionBlockedError('segregation_violation','/roles',`${a}/${b}`); const authorizers=new Set(assignments.filter(a=>a.role==='final_authorizer'&&a.real===true).map(a=>a.identity)); if(authorizers.size!==policy.required_authorizer_count)throw new PromotionBlockedError('authorization_approval_missing','/authorizers','exactly one real authorizer required'); return true; }
